import React from 'react'
import { IMovie } from '../models/movis'

export type FilterProps = {
    movies: IMovie
  }

const filterMovies = ({ movies }: FilterProps) => {
  return (
    <div>
      
    </div>
  )
}

export default filterMovies
